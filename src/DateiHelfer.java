import java.io.IOException;
import java.nio.file.Files;
import java.nio.file.Paths;
import java.nio.file.StandardOpenOption;

public class DateiHelfer {/*

    public class DateiHelfer {
        public static String dateiLesen(String dateiPfad) throws IOException {
            return new String(Files.readAllBytes(Paths.get(dateiPfad)));
        }

        public static void dateiSchreiben(String dateiPfad, String inhalt) throws IOException {
            Files.write(Paths.get(dateiPfad), inhalt.getBytes());
        }

        public static void dateiSchreiben(String dateiPfad, String inhalt, StandardOpenOption option) throws IOException {
            Files.write(Paths.get(dateiPfad), inhalt.getBytes(), option);
        }
    }
*/
}
